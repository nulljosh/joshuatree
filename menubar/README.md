# Joshua Tree Monitor

Menu bar app showing live build progress: `X/Y shipped` from `roadmap.md`'s
checkboxes, plus the latest commit. Same pattern as Conveyer's
ConveyerMonitor (`~/Documents/Code/conveyer/menubar`).

```sh
./build.sh   # builds, ad-hoc signs, relaunches
```
